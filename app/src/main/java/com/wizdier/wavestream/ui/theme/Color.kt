package com.wizdier.wavestream.ui.theme

import androidx.compose.ui.graphics.Color

// ─── WaveStream Beta Palette ──────────────────────────────────────────────
// Deep ocean with electric cyan accents — inspired by CloudStream's dark
// aesthetic but pushed further with depth and vibrance.

// Primary brand — electric indigo
val WavePrimary = Color(0xFF6366F1)
val WavePrimaryDark = Color(0xFF4338CA)
val WavePrimaryLight = Color(0xFF818CF8)

// Secondary — cyan accent for highlights, progress, active states
val WaveSecondary = Color(0xFF06B6D4)
val WaveSecondaryDark = Color(0xFF0E7490)
val WaveSecondaryGlow = Color(0xFF67E8F9)

// Tertiary — magenta for favorites, special actions
val WaveTertiary = Color(0xFFEC4899)
val WaveTertiaryGlow = Color(0xFFF9A8D4)

// Backgrounds — layered depth
val WaveBackground = Color(0xFF0A0E1A)         // deepest
val WaveBackgroundElevated = Color(0xFF111627) // cards
val WaveSurface = Color(0xFF1A2036)            // interactive surfaces
val WaveSurfaceVariant = Color(0xFF232B45)     // hovers, chips
val WaveSurfaceBright = Color(0xFF2D3654)      // pressed, selected

// On-colors
val WaveOnPrimary = Color(0xFFFFFFFF)
val WaveOnBackground = Color(0xFFF1F5F9)
val WaveOnBackgroundSecondary = Color(0xFFCBD5E1)
val WaveOnBackgroundMuted = Color(0xFF94A3B8)
val WaveOnSurface = Color(0xFFE2E8F0)
val WaveOnSurfaceVariant = Color(0xFF94A3B8)

// Accents
val WaveError = Color(0xFFEF4444)
val WaveSuccess = Color(0xFF22C55E)
val WaveWarning = Color(0xFFF59E0B)
val WaveInfo = Color(0xFF3B82F6)

// Outlines & dividers
val WaveOutline = Color(0xFF334155)
val WaveOutlineMuted = Color(0xFF1E293B)
val WaveDivider = Color(0xFF1E293B)

// Scrims — for overlays on top of images
val ScrimStrong = Color(0xF0000000)
val ScrimMedium = Color(0xCC000000)
val ScrimLight = Color(0x80000000)
val ScrimGradientTop = Color(0x66000000)
val ScrimGradientBottom = Color(0xE6000000)

// Shimmer — for loading placeholders
val ShimmerBase = Color(0xFF1A2036)
val ShimmerHighlight = Color(0xFF2D3654)
val ShimmerDurationMs = 1200

// Gradients — for hero sections and active states
val HeroGradientStart = Color(0xFF1E1B4B)
val HeroGradientEnd = Color(0xFF0A0E1A)
val ActiveNavGradientStart = Color(0xFF6366F1)
val ActiveNavGradientEnd = Color(0xFF06B6D4)
val PosterScrimTop = Color(0x00000000)
val PosterScrimBottom = Color(0xD9000000)

// Quality colors
val Quality4K = Color(0xFFD4AF37)    // gold
val Quality1080 = Color(0xFF06B6D4)  // cyan
val Quality720 = Color(0xFF818CF8)   // indigo
val Quality480 = Color(0xFF94A3B8)   // slate
